#  tz 1
supabase schema v1->
CREATE TABLE notes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID,
  content TEXT,
  created_at TIMESTAMP DEFAULT now()
);


# Try /test-db again.

# Now:
# 👉 it returns empty OR errors (because RLS blocks it)

# Good. That means:

# security is active
# your setup is real
# What you achieved (don’t underestimate this)

# You now have:

# running FastAPI
# Supabase connected
# DB working
# RLS enforced

# This is the hardest foundation step. Most people quit here.



#  tz 2
...
Level 1 — Auth + First Real Secure Insert

Goal:

users can sign up
login
get JWT
create THEIR own notes
RLS protects data
.
supabase schema v1->
CREATE TABLE notes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
  title TEXT,
  content TEXT,
  created_at TIMESTAMP DEFAULT now()
);

ALTER TABLE notes ENABLE ROW LEVEL SECURITY;





CREATE POLICY "Users can view own notes"
ON notes
FOR SELECT
USING (auth.uid() = user_id);


CREATE POLICY "Users can insert own notes"
ON notes
FOR INSERT
WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update own notes"
ON notes
FOR UPDATE
USING (auth.uid() = user_id);

CREATE POLICY "Users can delete own notes"
ON notes
FOR DELETE
USING (auth.uid() = user_id);
Now your DB is actually secure..............................

